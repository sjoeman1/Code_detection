```python
n = int(input())

boxes = []
for _ in range(n):
    k, a = map(int, input().split())
    boxes.append((k, a))

boxes.sort(key=lambda x: x[0])

p = 0
for i in range(len(boxes)):
    if boxes[i][0] > boxes[i-1][0]:
        p = boxes[i][0]
        break

print(p)
```