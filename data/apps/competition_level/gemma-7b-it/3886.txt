```python
q = int(input())

for _ in range(q):
    n, k = map(int, input().split())
    f = "What are you doing at the end of the world? Are you busy? Will you save us?"
    s = f + f" " * n
    print(s[k-1])
```