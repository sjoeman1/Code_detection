```python
n = int(input())
a = list(map(int, input().split()))

a.sort()

min_diff = float('inf')
for i in range(len(a) - 1):
    diff = abs(a[i] + a[i + 1] - a[i])
    min_diff = min(min_diff, diff)

print(min_diff)
```