```python
n = int(input())
a = list(map(int, input().split()))

a_max = max(a)
a_min = min(a)

for i in range(n):
    a_i = int(a[i])
    if a_i < 0:
        a_max = max(a_max, -a_i - 1)
    else:
        a_max = max(a_max, a_i + 1)

print(*(-a_max - 1) * n)
```