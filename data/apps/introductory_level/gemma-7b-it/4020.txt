```python
h1, m1 = map(int, input().split(":"))
h2, m2 = map(int, input().split(":"))

# Calculate the midpoint of the contest
h3 = (h1 + h2) // 2
m3 = (m1 + m2) // 2

# Print the midpoint
print(str(h3) + ":" + str(m3))
```